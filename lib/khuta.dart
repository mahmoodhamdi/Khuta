import 'package:flutter/material.dart';

class Khuta extends StatelessWidget {
  const Khuta({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(title: 'Khuta', home: Scaffold());
  }
}
